databases:
  - name: roadwatch-db
    databaseName: roadwatch
    user: roadwatch
    plan: free

services:
  # ── Backend (FastAPI) ──────────────────────────────────────────────────────
  - type: web
    name: roadwatch-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: roadwatch-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: roadwatch-redis
          type: redis
          property: connectionString
      - key: GROQ_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: ENVIRONMENT
        value: production
      - key: CHROMA_HOST
        value: localhost
      - key: FRONTEND_URL
        fromService:
          name: roadwatch-frontend
          type: web
          property: host

  # ── Frontend (React Static Site) ──────────────────────────────────────────
  - type: web
    name: roadwatch-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    plan: free
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      - path: /sw.js
        name: Cache-Control
        value: no-cache
    envVars:
      - key: VITE_API_URL
        fromService:
          name: roadwatch-api
          type: web
          property: host

  # ── Redis ──────────────────────────────────────────────────────────────────
  - type: redis
    name: roadwatch-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []          # empty = only Render internal services can connect (most secure)
